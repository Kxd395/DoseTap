# SSOT Pending Items

- UI component IDs pending binding to real UI: `bulk_delete_button`, `date_picker`, `delete_day_button`, `devices_add_button`, `devices_list`, `devices_test_button`, `heart_rate_chart`, `insights_chart`, `session_list`, `settings_target_picker`, `timeline_export_button`, `timeline_list`, `tonight_snooze_button`, `wake_up_button`, `watch_dose_button`.
- WHOOP/OAuth: disabled by default until token handling and entitlement review are complete.
- HealthKit: NoOp provider on simulator/unsigned builds; enablement requires entitlements and user opt-in.
